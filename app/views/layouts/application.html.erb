<!DOCTYPE html>
<html>
  <head>
    <title>Task</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/e99d188784.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="p-2 shadow-sm border-bottom border-dark">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <%= link_to "Task", root_path, class: "navbar-brand text-dark" %>
        <div>
          <% if user_signed_in? %>
            <p>ようこそ<%= current_user.last_name %>さん！</p>
          <% end %>
        </div>
        <div>
          <nav class="mb-2">
            <ul class="navbar-nav list-group-horizontal">
              <li class="nav-item"><%= link_to "About", "/about", class: "btn btn-outline-secondary mr-3" %></li>
              <li class="nav-item"><%= link_to "My Task", "/todos", class: "btn btn-outline-secondary mr-3" %></li>
              <li class="nav-item"><%= link_to "新規登録", "/users/sign_up", class: "btn btn-outline-secondary mr-3" %></li>
              <li class="nav-item"><%= link_to "ログイン", "/users/sign_in", class: "btn btn-outline-secondary mr-3" %></li>
            </ul>
          </nav>

        </div>
      </div>
    </header>
    <main class="mb-auto">
      <p id="notice"><%= notice %></p>
      <%= yield %>
    </main>

    <footer class="border border-dark fixed-bottom pt-5 pb-4">
    </footer>
  </body>
</html>
